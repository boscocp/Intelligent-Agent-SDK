using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class AgenteManager : MonoBehaviour, IAgenteManager
{
    // Start is called before the first frame update
    public IAgente RecuperarAgente(IAgente agenteSO)
    {
        //Debug.Log("agente colocado na posicao "+((AgenteScriptableObject)agenteSO).posicao);
        return null;
    }
}
